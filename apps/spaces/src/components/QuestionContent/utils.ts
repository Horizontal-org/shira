import { EmailContent } from "../QuestionFlowManagement/types"


// export const parseContent = (content: EmailContent | any, contentType: string) => {
//   let parsedContent = null
//   if (contentType === 'email') {
//     parsedContent = parseEmailContent(content)    
//   }

//   return parsedContent
// }

// const editorId = `component-text-${componentId}`
// component-required-sender-name
// component-required-sender-email
// component-optional-subject
export const parseEmailContent = (content) => {
  
}