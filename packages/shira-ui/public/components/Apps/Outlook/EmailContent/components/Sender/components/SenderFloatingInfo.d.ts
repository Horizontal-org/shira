import { FunctionComponent, ReactElement } from 'react';
interface Props {
    senderName: string;
    senderEmail: string;
    children: ReactElement;
}
export declare const SenderFloatingInfo: FunctionComponent<Props>;
export {};
