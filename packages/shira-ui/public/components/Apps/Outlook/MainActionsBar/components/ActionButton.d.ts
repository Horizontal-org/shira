import { FunctionComponent, ReactNode } from 'react';
interface Props {
    children: ReactNode;
    chevron: boolean;
    icon: ReactNode;
    hide?: string;
}
export declare const ActionButton: FunctionComponent<Props>;
export {};
