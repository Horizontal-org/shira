import { FunctionComponent } from 'react';
interface Props {
    phone?: {
        textContent: string;
        explanationPosition: string;
    };
}
declare const Recipient: FunctionComponent<Props>;
export default Recipient;
