import { FunctionComponent, ReactNode } from 'react';
interface Props {
    children: ReactNode;
    className?: string;
    background: string;
}
export declare const Phone: FunctionComponent<Props>;
export {};
