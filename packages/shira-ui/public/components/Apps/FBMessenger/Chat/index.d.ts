import { FunctionComponent } from 'react';
interface Props {
    fullname: {
        textContent: string;
        explanationPosition: string;
    };
    content: HTMLElement;
}
declare const Chat: FunctionComponent<Props>;
export default Chat;
