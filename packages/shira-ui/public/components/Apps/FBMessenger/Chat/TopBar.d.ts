import { FunctionComponent } from 'react';
interface Props {
    fullname: {
        textContent: string;
        explanationPosition: string;
    };
}
declare const TopBar: FunctionComponent<Props>;
export default TopBar;
